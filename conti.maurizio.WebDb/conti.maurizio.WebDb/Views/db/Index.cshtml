@using System.Data
@model DataTable


<h2>Categories</h2>

<table class="table table-hover" >

    <tr>
        <th>ID</th><th>Nome Prodotto</th><th>Descrizione</th>
    </tr>

    @foreach( DataRow item in Model.Rows )
    {
        //byte[] buff = (byte[])item["Picture"];
        //string imageBase64 = Convert.ToBase64String(buff);
        //string imageSrc = string.Format("data:image/gif;base64,{0}", imageBase64);

        <tr>
            <td>@item["CategoryID"]</td>
            <td>@item["CategoryName"]</td>
            <td>@item["Description"]</td>
            <td>
                @*<img src="data:image/jpg;base64,@(Convert.ToBase64String(item["Picture"]))" />*@
                @*<img src="@imageSrc" />*@
                @*<img src=" @{ var f = (byte[])item["Picture"]; string base64String = Convert.ToBase64String(f); String.Format("data:image/png;base64,{0}", base64string) " /> }></td>*@

        </tr>
    }

</table>
